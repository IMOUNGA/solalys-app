# L'upload d'images passe désormais uniquement par l'API (/v1/upload/image),
# qui détient les clés Cloudflare R2 côté serveur. L'app ne doit plus avoir
# de clés R2 en direct (secret exposé dans le bundle sinon).
