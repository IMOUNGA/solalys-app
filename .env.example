# L'upload d'images passe par l'API (POST /v1/upload/image), qui détient les
# clés Cloudflare R2 côté serveur (voir solalys-api/.env). L'app mobile n'a
# besoin d'aucune clé R2 en direct.

EXPO_PUBLIC_API=http://127.0.0.1:3000
