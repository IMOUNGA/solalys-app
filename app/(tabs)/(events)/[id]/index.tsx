// Même écran que (trouver)/[id]/index.tsx, monté ici aussi pour que la
// navigation depuis "Mes événements" reste dans cet onglet (le bouton
// retour doit revenir à la liste "Mes événements", pas basculer vers Trouver).
export { default } from '../../(trouver)/[id]/index';
