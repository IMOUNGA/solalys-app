APP_ENV=prod
# EXPO_PUBLIC_API volontairement non défini ici : Expo charge ce fichier pour
# TOUT build release (pas juste le profil "production"), donc une valeur en dur
# ici écrasait la config preprod des builds testflight-preprod (bug trouvé le
# 2026-09-10 : tous les builds TestFlight pointaient vers prod par erreur).
# app.config.ts choisit déjà la bonne URL via APP_ENV (défini par profil EAS).
